package pt.iscte.dcti.poo.sokoban.starter;

import javax.swing.text.Position;

import pt.iul.ista.poo.utils.Direction;
import pt.iul.ista.poo.utils.Point2D;

public class Caixote extends Objetos implements ObjetosAnimados{
		
	public Caixote(Point2D position,SokobanGame jogo) {
		super(position);
	}


	@Override
	public String getName() {
		return "Caixote";
	}

	@Override
	public int getLayer() {
		return 2;
	}


	@Override
	public void move(Direction direction) {
		Position newPosition = (Position) getPosition().plus(direction.asVector());
		for(Objetos objecto : jogo.getObjetos())
			if(objecto.getPosition().equals(newPosition))
			{
				if(objecto.isTransposable())
				{
					position = newPosition;
				}
			}
		ImageMatrixGUI.getInstance().update();
	}
	
}
